<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" x:Class="Modules.Security.Views.LoginView"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel VerticalAlignment="Center" Margin="10 0 10 0">
        <TextBlock Foreground="Red" TextAlignment="Center" Text="{Binding ErrorText}" Margin="0 0 0 10"/>
        <dxe:TextEdit Text="{Binding Login}" NullText="Login" Margin="0 0 0 13"/>
        <dxe:PasswordBoxEdit NullText="******" Text="{Binding Password}"/>
    </StackPanel>
</UserControl>
